# jupyter

## Install

    $ pip install jupyter
    $ pip install jupyterlab
    $ pip install plotly
    $ jupyter labextension install jupyterlab-plotly

    $ pip install notebook

## Usage

    $ jupyter lab
