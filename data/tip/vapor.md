# vapor

## Usage

    $ vapor new <name> [--template]
        --template=api
        --template=web
        --template=user/repo
        --template=http://example.com/repo-path
        --branch=foo

    $ vapor build
    $ vapor run

    $ vapor build --release
    $ .build/release/Run --env production
